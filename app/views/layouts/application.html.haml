!!!
%html
  %head
    %title Splendid Bacon
    = stylesheet_link_tag "reset", "text", "960_24_col", "application"
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    - unless flash.empty?
      - flash.each do |key, value|
        %div{:class => "#{key} flash"}= value
    = yield
